"""EX06 Dictionary Functions."""

__author__ = "730481331"


def invert([dict[str, str]]) -> dict[str, str]:
    invert_dictionary:
    i: int = 0
    while i <= 
        for item in invert_dictionary:
    return dict[str, str]

def favorite_color([dict[str, str]]) -> str:
    dictionary: dict[str, str]
    


def count(list[str]) -> [dict[str, int]]:
    dictionary: dict[str, int]
    dictionary = dict()
    i = 0
    times: int = 1
    while i <= len(list):
        for list[i] in list[str]:
            list[i] = dictionary[list[i]] = times
        if list[i] in dictionary:
            dictionary[list[i]] = times + 1
        i += 1
    return dictionary 